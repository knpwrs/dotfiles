# Load zsh modules
autoload -U colors && colors

# Source everything ending in .zsh in the lib directory.
source $DOTFILESLIB/*.zsh

# Load syntax highlighting
source $DOTFILES/vendor/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
